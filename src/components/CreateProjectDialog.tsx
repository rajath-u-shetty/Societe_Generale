import React from 'react'

type Props = {
  organizationId: string;
}

const CreateProjectDialog = ({ organizationId }: Props) => {
  return (
    <div>CreateProjectDialog</div>
  )
}

export default CreateProjectDialog
